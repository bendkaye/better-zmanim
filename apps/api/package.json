{
  "name": "@better-zmanim/api",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "kosher-zmanim": "^0.5.0",
    "moment-timezone": "^0.6.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250327.0",
    "vitest": "^3.1.0",
    "wrangler": "^4.10.0"
  }
}
